# /run — 构建并运行项目

构建并运行 DynamicIslandLyrics WPF 应用。

## 用法

```
/run
/run [configuration]
```

## 参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `configuration` | 构建配置：`Debug` 或 `Release` | `Debug` |

## 示例

```
/run            # Debug 模式构建并运行
/run Release    # Release 模式构建并运行
```
